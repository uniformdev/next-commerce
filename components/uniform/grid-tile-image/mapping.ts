import { ResolveComponentResultWithType } from 'uniform/models';
import { GridTileImageComponent } from './';

export const gridTileImageMapping: ResolveComponentResultWithType = {
  type: 'gridTileImage',
  component: GridTileImageComponent
};
