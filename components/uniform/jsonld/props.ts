export type Parameters = {
  name: string;
  description: string;
  image: string;
  availability: boolean;
  highPrice: string;
  lowPrice: string;
  priceCurrency: string;
};

export type Slots = string;
