import { ResolveComponentResultWithType } from 'uniform/models';
import { JsonldComponent } from './index';

export const jsonldMapping: ResolveComponentResultWithType = {
  type: 'jsonld',
  component: JsonldComponent
};
