import { ResolveComponentResultWithType } from 'uniform/models';
import { GalleryComponent } from './index';

export const galleryMapping: ResolveComponentResultWithType = {
  type: 'gallery',
  component: GalleryComponent
};
