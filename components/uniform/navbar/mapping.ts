import { ResolveComponentResultWithType } from 'uniform/models';
import { NavBarComponent } from './index';

export const navBarMapping: ResolveComponentResultWithType = {
  type: 'navBar',
  component: NavBarComponent
};
